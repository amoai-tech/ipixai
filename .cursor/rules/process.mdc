---
description: >-
  iPixai process docs — conversion plan, Mastra/CopilotKit playbooks.
  Load when the user mentions process, playbooks, or architecture review.
alwaysApply: false
---

# Process playbooks (on demand)

This repo does **not** have old iPix `docs/process/` or `AGENTS.md` yet. Use:

| Doc | Use for |
|-----|---------|
| `docs/copilotkit-mastra/README.md` | Current CopilotKit + Mastra runtime contract |
| `docs/roadmap.md` | Now → Next → Later product direction |
| `docs/mastra/db-001-matrix.md` | `mastra` Postgres schema contract |
| `docs/archive/cursor/linux-audit.md` | Historical host OOM / DEV-STAB-001 evidence |

## What goes where

| Primitive | Use for |
|-----------|---------|
| **`.cursor/rules/*.mdc`** | Hard gates (`alwaysApply`) and on-demand entry points |
| **`docs/mastra/`** | Conversion and platform notes |
| **Skills** | Load from Cursor/Claude skills when the stack matches (Mastra, CopilotKit, Supabase) |

Do **not** dump playbooks into new alwaysApply rules.

## Sibling rules

| Topic | Rule |
|-------|------|
| Smallest change | `ponytail.mdc` |
| Lifecycle | `lean.mdc` |
| Evidence | `task-accuracy.mdc` |
| Linear | `linear-governance.mdc` |
| Dev servers | `ipixai-dev.mdc` |
