# Legacy task-verifier references

These files are preserved for historical/compatibility lookup only. They are **not** part of the current iPix verifier protocol and may contain old paths, providers, tasks, or lifecycle assumptions.

For new verification use:

1. `../../SKILL.md`
2. `../quick-gate.md` for Quick mode
3. `../task-spec-rubric.md` for Full scoring
4. `../anti-fake-done-checklist.md` for Done claims
5. `.claude/skills/tasks/SKILL.md` + affected domain skills

If a legacy file conflicts with current code/runtime, `AGENTS.md`, `tasks`, or live Linear, the current source wins.
