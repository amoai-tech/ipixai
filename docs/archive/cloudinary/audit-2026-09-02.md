# Cloudinary V2 audit — 2026-09-02 (corrected)

**Photo-lab analog:** the darkroom (Cloudinary) already exists. The proof-room door (Supabase) still has a hotel master key on the *lock cylinder* (GRANT). RLS is a second lock on some rooms, not a replacement for that cylinder.

**SSOT:** live Linear [Cloudinary V2 view](https://linear.app/amo100/view/cloudinaryv2-33557daa5a18) · tracker [IPI-1123](https://linear.app/amo100/issue/IPI-1123). This file is a dated probe, not a second tracker.

**Production-ready: no.** Pipeline **~28–30/100** is a **provisional planning score** (custom weights), not an externally measured metric.

---

## Five corrections (2026-09-02 review)

| Was wrong | Correct |
| --- | --- |
| Merge PR #40 after IPI-1122 | **Merge [PR #40](https://github.com/amoai-tech/ipixai/pull/40) now.** CI/Vercel/CodeRabbit are green. Independent of DDL. |
| “anon can TRUNCATE” | Roles **possess** TRUNCATE/DML grants. Current RLS **blocks ordinary anon row DML**. Still a least-privilege fail. **Stronger live risk:** `authenticated` has matching INSERT/UPDATE/DELETE **policies** on `cloudinary_assets`, so signed-in members can mutate the provider mirror today. |
| `NEXT_PUBLIC_CLOUDINARY_API_KEY` is architectural | Browser needs the **public API key** for the signed Widget ([Upload Widget](https://cloudinary.com/documentation/upload_widget)). Exact env **name** is **IPI-1110** implementation. |
| Close IPI-1109 immediately | Close only after attaching live evidence + **FAIL → IPI-1122** handoff. Audit purpose; unsafe result can be Done. |
| 28/100 as hard truth | Keep as **provisional**. |

Preset rows below are from local `user-cloudinary-*` / CLI (`ipix-cloudinary`). Hosted Cloudinary plugin still returns **`cloud_name mismatch`**.

---

## Faster path (corrected order)

```text
NOW — parallel, no dependency conflict
├─ Merge PR #40 → verify main has cloudinary + next-cloudinary + server-only (**done** `e282ba7`)
├─ Recertify IPI-1040 (dry-run only; no repair)
└─ Close IPI-1109 as FAIL → IPI-1122 evidence owner

                 ↓ all green
IPI-1122  (hard DB gate)
                 ↓
IPI-1110 ∥ IPI-1111 ∥ IPI-1112
                 ↓
IPI-1113 ∥ IPI-1114 → 1116 ∥ 1069 → 1118 → 1136 ∥ 1138 → 1119 → 1120 → 1115 LAST
```

Do **not** serialize PR #40 behind database work. Do **not** start signed production uploads against the current provider-mirror contract.

Official path after 1122: `CldUploadWidget` + `cloudinary.utils.api_sign_request` ([signed Widget](https://cloudinary.com/documentation/upload_widget), [App Router example](https://github.com/cloudinary-community/cloudinary-examples/tree/main/examples/nextjs-clduploadwidget-signed)). Ignore Cloudinary Next skill “default unsigned.” Keep `www.ipix.co` until **IPI-1115**. Authenticated originals **and** derived assets need signed access; no arbitrary on-the-fly transforms ([control access](https://cloudinary.com/documentation/control_access_to_media)). Grants and RLS are separate gates ([Securing the Data API](https://supabase.com/docs/guides/api/securing-your-api)).

---

## Task grades (provisional)

Composite = `0.35×spec + 0.40×execution + 0.25×skills`. Execution is proven behavior, not Linear status.

| Task | Linear | Spec | Exec | Comp | Verdict |
| --- | --- | ---: | ---: | ---: | --- |
| **IPI-1040 · MIGRATION-001** | In Progress | 99 | 70 | **83** | Recertify in parallel with #40. Blocks 1122 DDL only. |
| **IPI-1108 · CLD-FOUNDATION-001** | Done | 88 | 55 | **73** | Fake-Done until #40 is on `main`. Then ~98. |
| **IPI-1109 · MEDIA-DATA-001** | Done (FAIL) | 92 | 70 | **79** | Closed as evidence owner. DDL is 1122. |
| **IPI-1122 · SB-MEDIA-HARDEN-001** | Todo | 95–97 | 15 | **55** | Spec correct. Hard gate before 1110/1111/1112. |
| **IPI-1110 · CLD-SIGN-001** | Todo | 93 | 0 | **42** | Official `api_sign_request` + allowlist. Blocked by **1122**, not by #40-after-1122. |
| **IPI-1111 · CLD-WEBHOOK-001** | Todo | 90 | 0 | **40** | Keep prod URL until 1115. |
| **IPI-1112 · CLD-DELIVERY-001** | Todo | 92 | 0 | **41** | Signed authenticated eager URLs. |

**Will the plan succeed?** 🟡 **YES AFTER** merge #40 + 1040 recertify + 1109 closeout + one 1122 migration. Architecture ~96/100. Do not claim 100 until ACs have live proof.

---

## Live fashionos (read-only) — GRANT vs RLS

RLS is **on**. GRANT is still too wide. [Grants decide whether a role may touch the object; RLS which rows](https://supabase.com/docs/guides/api/securing-your-api).

| Table | Table GRANT | Ordinary row DML today |
| --- | --- | --- |
| `public.assets` | `anon` + `authenticated` include SELECT/INSERT/UPDATE/DELETE/**TRUNCATE/TRIGGER** | `anon` row DML blocked by RLS. Least-privilege still fails. |
| `public.cloudinary_assets` | same broad grants | **`authenticated` INSERT/UPDATE/DELETE policies exist — mirror mutation is real** |
| `public.asset_events` | `authenticated` SELECT only | Keep narrow |

**FK:** `public.assets.shoot_id` → **`public.shoots`**, not `shoot.shoots`.  
**Comment still wrong:** approval → public `upload`. Keep Cloudinary `authenticated`.  
**`get_brand_assets`:** `SECURITY DEFINER`, `authenticated` EXECUTE.

---

## Live Cloudinary (local MCP / CLI — not hosted plugin)

| Preset | Use |
| --- | --- |
| `ai_powerstart` | Unsigned **validation only** |
| `ipix-signed-upload` | Production operator: signed, `type=authenticated`, eager `f_auto,q_auto` |
| `ipix_manual_moderation` | Public `upload` — **do not** wire operator widget |

---

## Linear: mint vs correct

**Do not mint new tickets.** Specs for 1122/1110/1111/1112 are already the official path. Corrections: tracker order, 1109 closeout, GRANT/RLS wording, 1110 public-key env name, 1108 Done inconsistency (comment until merge).

---

## Production-ready checklist

- [ ] PR #40 merged; `main` contains `cloudinary`, `next-cloudinary`, `server-only`
- [ ] IPI-1040 current-main CLI dry-run PASS; ledger unchanged
- [ ] IPI-1109 evidence archived; FAIL → 1122
- [ ] `anon` has no private DAM access
- [ ] `authenticated` cannot mutate Cloudinary identity/version/status
- [ ] no unnecessary client TRUNCATE/TRIGGER
- [ ] trusted active-org A/B isolation
- [ ] canonical V2 shoot link cannot cross brand/org
- [ ] `get_brand_assets` cannot bypass active-org
- [ ] approval never changes Cloudinary `authenticated`
- [ ] signed Widget: public API key + **server-generated** signature; secret server-only
- [ ] webhook verifies signature; 200 only after persist; idempotent
- [ ] approval binds exact `cloudinary_asset_id + version`; new version does not inherit
- [ ] private delivery = signed authenticated eager/approved derivatives
- [ ] disposable E2E; **IPI-1115 last**
