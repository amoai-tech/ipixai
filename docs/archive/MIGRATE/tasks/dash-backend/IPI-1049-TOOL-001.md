# IPI-1049 · TOOL-001 — Let the Planner Build Shoot Type, Deliverables, Shot List, and Budget Safely

**File:** `IPI-1049-TOOL-001.md`  
**Linear action:** UPDATE  
**MIGRATEv2:** Yes  
**READY TO PATCH LINEAR:** YES  
**Audit score:** 94→99/100 after contract corrections

## 0. Faster / better — FIRST/default method

```text
PLANNER-001 green
→ mine four Lumina deterministic cores
→ separate assumptions from confirmed facts
→ make trusted shot-reference dependency explicit input
→ thin current createTool wrappers
→ pure/schema tests
→ Planner tool-call integration
```

No Supabase migration/RPC, network lookup, write path, HITL, Cloudinary, booking, payment, or publishing logic belongs here.

## 1. Current V2 scope

Exactly four compute-only planning tools:
1. `recommendShootType`
2. `planDeliverables`
3. `generateShotListDraft`
4. `estimateShootBudget`

Current `ipixai/main` only has the starter weather tool, so this is a genuine migration gap.
## 2. Exact LuminaAI sources to inspect

Tool registry:
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/index.ts

Exact tool implementations:
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/recommendShootType.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/planDeliverables.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/generateShotListDraft.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/estimateShootBudget.ts

Discovery/tests only:
- https://github.com/amoai-tech/luminaai/tree/main/app/src/mastra/tools

## 3. COPY

- deterministic scoring/calculation cores
- Zod input/output schemas where still valid
- channel/format mappings
- shot-list-from-reference behavior and warnings
- budget line-item structure, not legacy rates as truth
- focused fixtures, validation and negative tests

## 4. ADAPT

- current `@mastra/core` createTool API and installed Zod types
- shoot-type low-confidence case must return explicit uncertainty/needs-input rather than silently defaulting to ecommerce
- deliverable quantities/defaults must be labeled `assumed` / `ipix_default` until operator-confirmed
- budget calculator must separate arithmetic from market assumptions; rates/currency are explicit validated inputs or clearly marked defaults
- `generateShotListDraft` must accept validated `trustedReferenceShotTypes` as input; it must not fetch them itself

## 5. Trusted shot-reference contract

Legacy Lumina requires `lookupShotReferences → generateShotListDraft`, but TOOL-001 must remain four compute-only tools with no DB/network access.

Required V2 contract:

```text
generateShotListDraft(
  approvedDeliverables,
  trustedReferenceShotTypes
)
```

Resolving/loading reference rows is outside this pure compute layer. A trusted upstream read/context owner must supply them before invocation. No invented angle names.

## 6. DROP

- `saveApprovedShootDraft`
- `approveShotList` mutations / old HITL runtime
- `lookupShotReferences` / `lookupChannelSpecs` as compute tools in this task
- Supabase/Postgres/service-role clients
- fetch/network
- Cloudinary/payments/publishing/booking/CRM
- old Worker/runtime wrappers

## 7. Dependencies

Hard: `IPI-1048 · PLANNER-001 — Make the Production Planner the Main iPix AI Assistant` must be green first.
## 8. Acceptance criteria

- [ ] exactly four initial compute tools
- [ ] Zod input/output contracts
- [ ] no Supabase/Postgres/service-role/fetch/network/Cloudinary/payment/publish imports
- [ ] retry-safe / deterministic where expected
- [ ] unknown shoot type handled safely
- [ ] deliverable defaults marked assumptions
- [ ] budget rates/currency explicit assumptions or inputs
- [ ] shot-reference dependency explicit and validated
- [ ] no invented shot angles
- [ ] focused unit/schema tests green
- [ ] canonical Planner invokes all four tools
- [ ] Planner does not claim results were saved
- [ ] typecheck + build + authenticated tool-call smoke green

## 9. Verification

`static forbidden-import audit → pure tests → schema boundaries → Planner integration → runtime regressions → typecheck → build → authenticated smoke`

No live Supabase before/after proof is required because these four tools must not touch application data.

**Will task succeed? YES after the reference-input and assumption contracts above are locked.**

## 10. READY TO PATCH LINEAR

**YES.** Prepend the 2026-09-03 contract corrections; keep exactly four compute tools.
