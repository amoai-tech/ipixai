# IPI-1065 · APP-001 — Give Operators One Consistent iPix Workspace Across the App

**File:** `dash-backend/IPI-1065-APP-001.md`  
**Linear action:** UPDATE (certification addendum)  
**MIGRATEv2:** Yes (already)  
**Implementation status:** **MERGED** (PR #43) — current work is **certify → Done**, not rebuild  
**Audit:** 2026-09-03 · implementation **97/100** · Linear freshness was **82/100** before addendum

---

## 0. Faster/better approach — FIRST/default method NOW

Do **not** copy Lumina shell or re-implement layout/nav.

```text
1. Fetch current origin/main
2. Verify APP-001 files still exist (layout, OperatorPanel, nav, CSS, placeholders)
3. Run APP-001 targeted tests
4. Typecheck
5. Build (ports 3000/4111 free)
6. Browser:
   signed-out /app → login
   signed-in /app → shell
   ≥3 routes retain shell
   / does not use operator shell
   ~390px menu works (inert + not keyboard-focusable when closed)
7. Confirm Planner “Open Planner” link is intentionally temporary (`/` today)
8. Confirm downstream tasks consume shell instead of rebuilding it
9. Attach hosted CI / exact SHA evidence if available
10. Mark APP-001 Done when certification evidence is sufficient
```

**Faster path:** treat APP-001 as **certification-only**. Downstream Brand/Shoot/Assets/CRM/… must **not** wait for an APP redesign.

---

## 1. What is already correct on `main`

- `src/app/app/layout.tsx` — `requireAppWorkspace()` → `OperatorPanel` → children; dynamic workspace
- Auth reused via `getVerifiedOperatorFromCookies()`; layout = shell access only; domain routes still authorize themselves
- Centralized `OPERATOR_NAV` (Home/Brands/Shoots/Assets/CRM/Talent/Operations/Analytics/Plans)
- Honest placeholders — no fake business data; unknown sections 404
- `/app` center owned by **DASH-MAIN-001** (not marketing home)
- Mobile a11y fixes: `inert` + `visibility:hidden`, scroll, reduced-motion, tests
- Lumina `OperatorPanel` was **not** copied wholesale (correct)

---

## 2. Linear corrections (addendum only — do not rewrite history)

| Stale wording | Correct current semantics |
| --- | --- |
| “no OperatorPanel / no shell” | Shell exists on `main` |
| “Implement `src/app/app/layout.tsx`” | File merged — verify only |
| “Owns route-context L1–L5” | Shell placement only; Brand/Shoot context → **IPI-1087 · PLANNER-CONTEXT-001** |
| Rail diagram → CopilotKit/AG-UI/Mastra | APP owns **layout slot**; Planner/CopilotKit content is downstream (UI-001 / Rail) |
| Permanent Planner at `/` | Temporary link until Planner has a non-`/` product route (before MARKETING-HOME takes `/`) |

---

## 3. Acceptance — certification checklist

### Already proven (implementation)

- [x] Persistent `/app/*` layout
- [x] One OperatorPanel + centralized nav
- [x] Children content slot + rail layout slot
- [x] Signed-out blocked; authenticated allowed
- [x] No browser service-role; no tenant/domain logic in shell
- [x] No fake domain data; marketing `/` outside shell
- [x] Mobile nav + focus fix + scroll + reduced motion
- [x] Invalid shell route 404
- [x] PR merged; review findings fixed

### Remaining for Done

- [ ] Exact current-main hosted certification attached (or honest N/A with local+PR evidence)
- [ ] Linear description refreshed to post-merge certification semantics
- [ ] Final Planner route replaces temporary `/` link before MARKETING-HOME takes root (owned primarily by UI-001 / marketing integration — not APP reopen)

---

## 4. Explicit non-goals

- Do not redesign OperatorPanel
- Do not mount full CopilotKit in the rail for APP Done
- Do not implement Brand/Shoot entity context transport
- Do not rename `/app`
- Do not block Wave A dashboard tasks on APP “polish”

---

## 5. Linear patch intent

Prepend:

> **AUTHORITATIVE POST-MERGE STATUS — 2026-09-03**  
> Implementation merged. Current work = **certification only**. Do not redesign. Downstream dashboards may build on current `main` shell now.

---

# AUTHORITATIVE FULL-URL + PRODUCTION AUDIT REFRESH — 2026-09-03

**Code authority:** audit and implement from a clean/current `amoai-tech/ipixai@main` / `origin/main`, not the current dirty docs worktree. At audit time local HEAD was `dbc6f0b...` while `origin/main` was `b034423...`. Re-check SHA at task start.

**Global execution rule:** inspect `package.json` + installed source/types first; current package/runtime contracts beat stale issue pins. The current skill tree has no `ponytail` skill, so use explicit cheapest-proof-first verification instead.

**Official implementation authority when relevant:**
- https://github.com/vercel/next.js
- https://github.com/supabase/supabase
- https://github.com/mastra-ai/mastra
- https://github.com/CopilotKit/CopilotKit

Use the official repository only to resolve current framework/library behavior; installed versioned source/types in ipixai still win when upstream `main` has moved ahead.

## Exact Lumina sources
- https://github.com/amoai-tech/luminaai/blob/main/app/src/components/operator-panel/operator-panel.tsx
- https://github.com/amoai-tech/luminaai/blob/main/app/src/app/(operator)/layout.tsx
- https://github.com/amoai-tech/luminaai/blob/main/app/src/app/layout.tsx
- https://github.com/amoai-tech/luminaai/blob/main/app/src/app/(marketing)/layout.tsx

## Current ipixai proof
- https://github.com/amoai-tech/ipixai/blob/main/src/app/app/layout.tsx
- https://github.com/amoai-tech/ipixai/blob/main/src/components/operator-panel/nav.ts

## Faster/better approach
**Certification only.** APP-001 is Done in live Linear. Do not rebuild shell; only recertify current main when a downstream task depends on it.

## Red flags / fixes
- Reopening APP for downstream UI → reject unless current-main regression is proven.
- Lumina shell wholesale copy → unnecessary and riskier than existing ipixai shell.
- `/app` label still `Home` → DASH-MAIN owns display-label correction.

## Score / production gate
Implementation 97/100 · task contract **99/100**. Success = current main shell/auth/mobile behavior remains green; no downstream task duplicates shell/auth.

