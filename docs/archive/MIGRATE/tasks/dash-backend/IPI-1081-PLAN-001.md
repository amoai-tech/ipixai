# IPI-1081 · PLAN-001 — Make the Planner Return a Complete Structured Shoot Plan

**File:** `IPI-1081-PLAN-001.md`  
**Linear action:** UPDATE  
**MIGRATEv2:** Yes  
**READY TO PATCH LINEAR:** YES  
**Audit score:** 92→99/100 after contract corrections

## 0. Faster / better — FIRST/default method

```text
BRAND + SHOOT + PLANNER + TOOL green
→ inventory Lumina plan/tool schemas + fixtures
→ define one versioned canonical ShootPlanSchema
→ compose trusted context + four TOOL outputs
→ validate final artifact
→ prove APPROVAL + SAVE can import the same type
```

Do not add another agent, duplicate tool logic, old workflow runtime, prose parsing, or application DB writes.

## 1. Current V2 scope

One canonical structured `ShootPlanSchema` and inferred TypeScript type consumed by Planner output, APPROVAL-001 and SHOOT-SAVE-001.

Unknown production facts must stay explicit as `confirmed`, `assumed`, or `needs_input` rather than becoming model-generated truth.
## 2. Exact LuminaAI sources to inspect

Primary legacy workflow/schema source:
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/workflows/shoot-wizard.ts

Primary workflow tests:
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/workflows/shoot-wizard.test.ts

Workflow discovery only:
- https://github.com/amoai-tech/luminaai/tree/main/app/src/mastra/workflows

Required tool/schema sources:
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/recommendShootType.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/planDeliverables.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/generateShotListDraft.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/estimateShootBudget.ts
- https://github.com/amoai-tech/luminaai/blob/main/app/src/mastra/tools/index.ts

## 3. COPY

- deliverable / shot / budget field shapes where still valid
- stage ordering and trusted-reference validation concepts
- product-category constraints
- representative fixtures and validation tests
- failure/refusal behavior
## 4. ADAPT

- create one V2 `ShootPlanSchema`; Lumina workflow schemas are input evidence, not authority
- add `schemaVersion: z.literal("1")` for MVP compatibility
- TOOL-001 outputs appear exactly once; no duplicate calculations
- trusted Brand/Shoot/operator context supplies remaining fields
- carry shot-reference provenance (source + trusted reference IDs)
- bound arrays, text, money, quantities and durations
- use explicit field status for uncertain values: `confirmed | assumed | needs_input`

Recommended uncertain-field shape:

```ts
{
  value: T | null,
  status: "confirmed" | "assumed" | "needs_input"
}
```

Use especially for location, studio, talent, crew, equipment, schedule and budget assumptions.

## 5. Required plan content

At minimum: purpose/objective, channels, shoot type, photo requirements, video requirements, deliverables, shot list, location, indoor/outdoor, lighting, set/background, talent, crew, studio, equipment, schedule, budget, risks, assumptions/open questions, schemaVersion, reference provenance.
## 6. DROP / corrections

- old `shoot-wizard` workflow runtime and suspend/resume transport
- old persistence/write steps and Cloudflare wrappers
- duplicate plan schemas across tools/UI/workflows
- prose/regex extraction fallback
- stale hardcoded Mastra pins; inspect installed package versions/source/types at task start
- mandatory `ponytail` skill reference — no such skill exists in the current `/home/sk/ipixai/.claude/skills` tree

Replace Ponytail wording with cheapest-proof-first/YAGNI verification:
`static inspection → targeted pure tests → integration → typecheck → build → live/runtime proof only when needed`.

## 7. Trusted shot-reference ownership

PLAN consumes `trustedReferenceShotTypes` required by TOOL-001; it must not assume they magically exist or let the model invent them. A trusted upstream read/context owner must resolve them before `generateShotListDraft` runs, and the final plan must retain reference provenance.

## 8. Dependencies

Hard blockers:
- `IPI-1068 · BRAND-001 — Let Operators Browse Brands and Open Complete Brand Profiles`
- `IPI-1067 · SHOOT-001 — Let Operators Browse Shoots and Open Complete Shoot Records`
- `IPI-1048 · PLANNER-001 — Make the Production Planner the Main iPix AI Assistant`
- `IPI-1049 · TOOL-001 — Let the Planner Build Shoot Type, Deliverables, Shot List, and Budget Safely`

Downstream: `IPI-1084 · APPROVAL-001 — Let Operators Review, Edit, Approve, or Reject AI Plans Before Anything Is Saved` → `IPI-1083 · SHOOT-SAVE-001 — Save an Approved Shoot Once and Under the Correct Organization`.
## 9. Acceptance criteria

- [ ] all four blockers Done
- [ ] exactly one exported `ShootPlanSchema` + inferred type
- [ ] `schemaVersion: "1"`
- [ ] required production fields present and bounded
- [ ] unknowns represented as confirmed/assumed/needs_input
- [ ] TOOL-001 outputs reused exactly once
- [ ] trusted shot-reference provenance retained
- [ ] no prose/regex parsing fallback
- [ ] no application DB writes
- [ ] APPROVAL imports the canonical plan type directly
- [ ] SAVE imports the canonical plan type directly
- [ ] installed model/provider tool→structured-output strategy proven; use smallest supported two-stage fallback if required
- [ ] signed-out/invalid-org fails through existing runtime gates
- [ ] cross-org Brand/Shoot/context injection fails closed
- [ ] invalid/partial plan cannot masquerade as complete
- [ ] targeted tests + typecheck + build + authenticated plan proof green

## 10. Verification

`schema/composition tests → TOOL integration/model compatibility → auth/org negatives → typecheck → build → authenticated Planner artifact → downstream type-import proof`

No production Supabase write is required.

**Will task succeed? YES after the reference owner and corrected contracts are locked.**

## 11. READY TO PATCH LINEAR

**YES.** Remove stale Mastra/Ponytail instructions and prepend the 2026-09-03 canonical-schema/reference-provenance corrections.
